# Server
PORT=5000
NODE_ENV=development

# WhatsApp (opsional — untuk notifikasi otomatis)
WA_PHONE=6281234567890

# Email (opsional — untuk notifikasi)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
NOTIFY_EMAIL=studio@lumorastudio.com
